name: MLOps CI/CD Pipeline

on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest

    - [cite_start]name: Run Tests [cite: 33]
      run: pytest tests/

    - name: Train Model
      run: python src/train.py

    # Login to Docker Hub (Requires Secrets)
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - [cite_start]name: Build and Push Docker Image [cite: 38]
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/heart-disease-api:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/heart-disease-api:latest